import "../styles/variables.sass";
import "../styles/components/navbar.sass";

const Navbar = () => {
    return (
        <div id="header">
            <header>
                <h1>Pokedex</h1>
            </header>
        </div>
    );
};

export default Navbar;
